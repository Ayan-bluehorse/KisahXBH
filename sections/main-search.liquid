<style>
#shopify-section-{{section.id}} .col-title{
     font-family:var(--base-font-family);
     font-style: normal;
     font-weight: 600;
     font-size: 24px;
     line-height: 26px;
     color: #000000;
     opacity: 0.8;
     margin-bottom: 5px;
 }
#shopify-section-{{section.id}} .product-grid{
    column-gap:15px;
    row-gap:0px;
}
#shopify-section-{{section.id}} .product-block__title{
  font-family: var(--base-font-family);
  font-style: normal;
  font-weight: 600;
  font-size: 16px;
  line-height: 18px;
  color: #000000;
  opacity: 0.8;
 
}
#shopify-section-{{section.id}} .col-description{
     margin-bottom:30px;
     margin-top:12px;
}
#shopify-section-{{section.id}} .col-description p{
    margin:0;
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    line-height: 18px;
    color: #000000;
    opacity: 0.8;
}
#shopify-section-{{section.id}} .close-container{
      display:none;
}
#shopify-section-{{section.id}} .link-dropdown__button-text{
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 21px;
    line-height: 23px;
    color: #000000;
    opacity: 0.8
}
/* #shopify-section-{{section.id}} .image-label-wrap .product-label-container {
    display: none;
} */
#shopify-section-{{section.id}} .product-block .collection-tag-label{
     position:absolute;
     top:15px;
     right:0;
     background: linear-gradient(90deg, #A68811 -15.48%, #C1A01E 49.84%, #836900 116.67%);
     border-radius: 50px 0px 0px 50px;
}
#shopify-section-{{section.id}} .product-block .collection-tag-label span{
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 11px;
    line-height: 12px;
    text-align: center;
    text-transform: uppercase;
    color: #FFFFFF;
    opacity: 0.8;
    padding: 3px 5px 4px 12px;;
    display:block;
}
 #shopify-section-{{section.id}} .product-label-container {
     position:unset;
     margin-inline-end:0.5rem;
} 
#shopify-section-{{section.id}} .no-js-hidden .product-label--sale span {
    font-family:var(--base-font-family);
    font-style: normal;
    font-weight: 600;
    font-size: 14px;
    line-height: 16px;
    color:#ff0606;
    /* opacity: 0.8; */
    padding:0;
    background:none;
    vertical-align:unset;
    display:block;
    text-transform:uppercase;
}
#shopify-section-{{section.id}} .link-dropdown__button[aria-expanded="true"]{
    background:#201747;
    height:auto;
}
#shopify-section-{{section.id}} .link-dropdown__button[aria-expanded="true"] .link-dropdown__button-text{
    color:#ffffff;
}
#shopify-section-{{section.id}} .link-dropdown__button[aria-expanded="true"] .link-dropdown__button-icon svg {
    color:#ffffff;
}
#shopify-section-{{section.id}} .text-overlay__inner{
      display:none;
 }
#shopify-section-{{section.id}} .breadcrumbs-collection{
     display:flex;
     margin-bottom:20px;
     margin-top:10px;
}
#shopify-section-{{section.id}} .breadcrumbs-collection .divider{
     list-style-type:none;
     margin:0 5px;
}
#shopify-section-{{section.id}} .utility-bar__left .btn-icon{
      order:2;
      margin-left:15px;
 }
#shopify-section-{{section.id}} .utility-bar__left span{
      font-family:var(--base-font-family);
      font-style: normal;
      font-weight: 400;
      font-size: 21px;
      line-height: 26px;
      color: #000000;
      opacity: 0.8;
}
#shopify-section-{{section.id}} .page-header--with-background{
     margin-bottom:0;
}
#shopify-section-{{section.id}} .toggle-btn{
      height:2.6rem;
      width:170px;
      padding:2px 20px;
}
#shopify-section-{{section.id}} .utility-bar__right .utility-bar__item {
      border: 1px solid #000000;
      border-radius: 10px;
      width:190px;
      height:2.6rem;
      padding:2px 20px;
      display:flex;
      align-items:center;
}
#shopify-section-{{section.id}} .utility-bar__right .utility-bar__item.special {
      background:#201747;
      border-radius:0;
}
#shopify-section-{{section.id}} .product-block__detail .price__current{
      font-family: var(--base-font-family);
      font-style: normal;
      font-weight: 400;
      font-size: 16px;
      line-height: 18px;
      color: #000000;
      opacity: 0.8;
}
#shopify-section-{{section.id}} .product-block__detail .price__was{
      font-family: var(--base-font-family);
      font-style: normal;
      font-weight: 400;
      font-size: 16px;
      line-height: 18px;
      color: rgba(52, 52, 52, 0.69);
      opacity: 0.8;
      text-decoration: line-through;
      text-decoration-color: rgba(255, 0, 0, 1);
}
#shopify-section-{{section.id}} .price .price-label--sold-out{
      font-family: var(--base-font-family);
      font-style: normal;
      font-weight: 600;
      font-size: 16px;
      line-height: 18px;
      color: #000000;
      opacity: 0.8;
}
#shopify-section-{{section.id}} .product-block__detail .product-price--block .coupon-price{
    margin-inline-start: var(--product-block-price-item-margin-start);
    margin-inline-end: var(--product-block-price-item-margin-end);
      font-family: var(--base-font-family);
      font-style: normal;
      font-weight: 400;
      font-size: 16px;
      line-height: 18px;
      color: #000000;
      opacity: 0.8;
}
/* #shopify-section-{{section.id}} .utility-bar__centre{
      display:none;
} */
#shopify-section-{{section.id}} .sort-by-title {
      display:none;
}

#shopify-section-{{section.id}} .link-dropdown{
      width:100%;
}
#shopify-section-{{section.id}} .link-dropdown__options{
     top:32px;
     background:#F3F3F3;
     padding-top:0;
     z-index:30;
    
}
#shopify-section-{{section.id}} .link-dropdown--right-aligned .link-dropdown__options{
      right:unset;
      bottom:0;
      left: -22px;
      width:192px;
      min-width:unset;
}
#shopify-section-{{section.id}} .link-dropdown__options .link-dropdown__link:not(:last-child){
    border-bottom:0.5px solid rgba(52, 52, 52, 0.83);
}
#shopify-section-{{section.id}} .link-dropdown__options .link-dropdown__link{
   background:#F3F3F3;
   font-family: var(--base-font-family);
   font-style: normal;
   font-weight: 400;
   font-size: 14px;
   line-height: 15px;
   color: #000000;
   padding: 15px 30px;

}
#shopify-section-{{section.id}} .link-dropdown__options .link-dropdown__link--active{
    background:#ffffff;
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 14px;
    line-height: 16px;
    color: #000000;
}
#shopify-section-{{section.id}} .link-dropdown .link-dropdown__button.has-motion{
    width:100% !important; 
}
#shopify-section-{{section.id}} .filter-group__heading__text{
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 600;
    font-size: 16px;
    line-height: 18px;
    color: #000000; 
}
#shopify-section-{{section.id}} .filter-group__item .filter-group__item__text{
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    line-height: 18px;
    color: #000000;
    opacity: 0.8;
}
#shopify-section-{{section.id}} #CollectionFilterForm{
   border-top: none;
   margin-top: 0;
}
#shopify-section-{{section.id}} #CollectionFilterForm .filter-group{
  border-bottom: 1px solid rgba(0, 0, 0, 0.3);
}
#shopify-section-{{section.id}} .filter-container--side .filter-group__item{
    font-family: var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    line-height: 18px;
    color: #000000;
    opacity: 0.8;
}
#shopify-section-{{section.id}} #CollectionFilterForm .filter-group__heading {
    padding-bottom: 10px;
    padding-top: 5px;
}

#shopify-section-{{section.id}} .utility-bar__left .toggle-btn .toggle-btn__chevron svg{
    transform: rotate(90deg);
}
#shopify-section-{{section.id}} .utility-bar__left .toggle-btn--revealed-desktop .toggle-btn__chevron svg{
    transform: rotate(180deg);
}
#shopify-section-{{section.id}} .filters__heading-text{
    Font-size: 18px;
    text-transform: uppercase;
    font-family: var(--base-font-family);
}

#shopify-section-{{section.id}} .mobile-whishlist{
     display:none;
}
#shopify-section-{{section.id}} .quickbuy-container .product-price--large .price__default {
  position:relative;
}
#shopify-section-{{section.id}} .quickbuy-container .product-label-container{
  position:absolute;
  top: 9.5px;
  right: -80px;
  bottom:unset;
  left:unset;
}
#shopify-section-{{section.id}} .utility-bar__centre .utility-bar__item {
    font-size: 16px;
    line-height: 18px;
    font-family: var(--base-font-family);
    color: #000000;
}
#shopify-section-{{section.id}} .btn--large{
    /* box-shadow: 0 4px 8px 3px #00000026, 0 1px 3px #0000004d; */
    border-radius: 50px;
    color: #fff;
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    line-height: 18px;
    text-align: center;
    font-family: var(--base-font-family);
    text-transform: capitalize;
    min-height: 40px;
    padding: 0 25px;
    letter-spacing: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #201747;
    width:100%;
    margin:12px 0;
}
/* #shopify-section-{{ section.id }} .product-block {
  height: 100%; 
}
#shopify-section-{{ section.id }} .product-block .block-inner {
    display: flex;
    flex-direction: column;
    align-items: stretch;
    height: 100%;
    justify-content: end;
}
#shopify-section-{{ section.id }} .product-block .block-inner-inner{
    display:flex;
    flex-direction:column;
    height:100%;
    align-items:stretch
}
#shopify-section-{{ section.id }} .product-block .image-cont{
    overflow:unset;
    height:100%;
    margin:unset;
}
#shopify-section-{{ section.id }} .product-block__detail{
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    justify-content:space-between;
    padding-top:10px;
}
#shopify-section-{{ section.id }} .product-block product-form.js-product{
   width:100%;
   margin-top:8px;
}
#shopify-section-{{ section.id }} .product-block .product-block__detail .inner{
    margin:0;
    width:100%;
} */
@media screen and (min-width:768.1px){
#shopify-section-{{section.id}} .filter-group__clear-row.mob-show{
    display:none;
}

#shopify-section-{{section.id}} .filter-group__clear-row{
    display:block;
}
}
@media screen and (max-width:768px){
#shopify-section-{{section.id}} .utility-bar__centre{
     order:2;
}
#shopify-section-{{section.id}} .utility-bar__centre .utility-bar__item {
    font-size: 14px;
    line-height: 16px;
    color: #ffffff;
    margin:0;
    padding-left:5px;
}
#shopify-section-{{section.id}} .col-description{
     text-align:center;
     margin-bottom:15px;
}
#shopify-section-{{section.id}} .text-overlay{
   padding:0;
 }
#shopify-section-{{section.id}} .img-fill .theme-img {
   position:unset;
   object-fit:contain;
}
#shopify-section-{{section.id}} .breadcrumbs-collection{
     margin-bottom:10px;
}
/* #shopify-section-{{section.id}} .desktop-whishlist{
     display:none;
} */
#shopify-section-{{section.id}} .mobile-whishlist{
     display:block;
}
#shopify-section-{{section.id}} .mobile-whishlist .wishlist-hero-custom-button {
     right:5px !important;
}
/* #shopify-section-{{section.id}} .product-label-container {
     display:none;
} */
#shopify-section-{{section.id}} .col-description p{
     font-size: 14px;
     line-height: 16px;
}
#shopify-section-{{section.id}} .filters__close.desk-version{
    display:none;
}
#shopify-section-{{section.id}} .filter-group--applied .filter-group__clear-link{
     display:none;
}
#shopify-section-{{section.id}} #CollectionFilterForm .filter-group{
     margin-bottom:0
}
#shopify-section-{{section.id}} .filter-group .toggle-target-container{
     padding:20px 4rem;
     background:#ffffff;
} 
#shopify-section-{{section.id}} .close-container{
      display:block;
      background:#201747;
      padding: 15px 0;
      position: sticky;
      top: 100%;
}
#shopify-section-{{section.id}} .close-container .filters__close{
     width:50%;
     text-align:center;
     
}
 #shopify-section-{{section.id}} .filter-group__heading__text{
     width:50%;
     text-align:center;
 }
#shopify-section-{{section.id}} .filter-group__heading__indicator{
     width:50%;
     text-align:center;
}
#shopify-section-{{section.id}} .utility-bar__right{
     order:1;
     width:35%;
     justify-content:center;
     flex:unset;
    
}
#shopify-section-{{section.id}} .utility-bar__left{
     order:3;
     width:35%;
     flex:unset;
     justify-content:center;
}
#shopify-section-{{section.id}} .link-dropdown__button-icon{
    left:50px;
    top:55%;
    right:unset
}
#shopify-section-{{section.id}} .desktop-mob-show {
    display: block !important;
}
#shopify-section-{{section.id}} .utility-bar__right .utility-bar__item.special {
      background:#201747;
      border-radius:20px;
}
#shopify-section-{{section.id}} .link-dropdown__button-icon svg {
      color:#ffffff;
}
#shopify-section-{{section.id}} .product-grid{
     column-gap:8px;
}
#shopify-section-{{section.id}} .utility-bar__right .mobile-only {  
     display:none;
}
#shopify-section-{{section.id}} .utility-bar{
    background:#201747;
    border-radius:20px;
    position:fixed;
    left:50%;
    transform:translate(-50%);
    z-index:999;
    min-width:85%;
    box-shadow: 0px 1px 3px rgba(0, 0, 0, 0.3), 0px 4px 8px 3px rgba(0, 0, 0, 0.15);
    border-radius: 50px;
    padding:3px 0;
    bottom:0;
}

#shopify-section-{{section.id}} .utility-bar__right .utility-bar__item {
    border:0;
    border-radius:0;
    width:unset;
    height: unset; 
    padding: 0;
}
#shopify-section-{{section.id}} .link-dropdown .link-dropdown__button.has-motion{
    width:unset !important;
   
} 
#shopify-section-{{section.id}} .utility-bar__left .btn-icon svg path{
    fill:#ffffff;
}
#shopify-section-{{section.id}} .col-title{
   text-align:center;
   font-size:16px;
   line-height:18px;
}

#shopify-section-{{section.id}} .filter-container .filters {
     bottom:0; 
     left: 0;
     transform: translateY(100%);
     top:unset;
     right:unset;
     padding:0; 
     background: #F3F3F3;
     height:90%;
     width: 100%; 
     overflow-y: scroll;
     overflow-x:hidden;
}
#shopify-section-{{section.id}} .filter-container.filter-container--show-filters-mobile .filters {
     transform: translateY(-3%);
} 
#shopify-section-{{section.id}} .sort-by-title {
    display: flex; 
    background: #201747;
    padding: 15px 30px;
    align-items: center;
   
}
#shopify-section-{{section.id}} .link-dropdown--right-aligned .link-dropdown__options {
    right:unset;
}
#shopify-section-{{section.id}} .sort-by-title p{
    font-family:var(--base-font-family);
    font-style: normal;
    font-weight: 400;
    font-size: 16px;
    line-height: 18px;
    text-transform: uppercase;
    color: #FFFFFF;
    margin:0;
}
#shopify-section-{{section.id}} .sort-by-title svg{
    width:20px;
    height:20px;
    margin-left:auto;
}
#shopify-section-{{section.id}} .toggle-btn{
    width:unset; 
    height:unset;
    padding:0;
    border:0;
    border-radius:0;
}
#shopify-section-{{section.id}} .utility-bar__right .link-dropdown__button-text{
    font-size:16px;
    color:#ffffff;
    opacity:1;
    
}
#shopify-section-{{section.id}} .utility-bar__left span{
     font-size:16px;
     color:#ffffff;
     opacity:1;
    
}
#shopify-section-{{section.id}} .link-dropdown .link-dropdown__button {
    height:auto;
    padding:0;
}
#shopify-section-{{section.id}} .cc-animate-init.cc-animate-in{
   position:relative;
}
#shopify-section-{{section.id}} .utility-bar__right .link-dropdown__options{
  transition: all 300ms ease;
  visibility:hidden;
  opacity:0;
  height: auto;
  width: 100vw;
  position: fixed;
  bottom: unset;
  top: unset;
  left: -28px;
  right: 0;
  overflow: visible;
  
  transform: translateY(200%);
}  
#shopify-section-{{section.id}} .link-dropdown__button[aria-expanded=true] + .link-dropdown__options {

  visibility:visible;
  opacity:1;  
  transform: translateY(-79%);
  left:-9%;
}
 
#shopify-section-{{section.id}} .filters__heading{
     background:#201747;
     padding: 15px 0 10px;
     margin:0;
}
#shopify-section-{{section.id}} .filters__heading .filters__heading-text{
      color:#ffffff;
      Font-size: 18px;
      text-transform: uppercase;
      font-family:var(--base-font-family);
       width:50%;
      text-align:center;
}
#shopify-section-{{section.id}} .filters__heading .filter-group__clear-row{
      width: 50%;
      margin: 0 auto;
      text-align: center;
}
#shopify-section-{{section.id}} .filters__heading .filter-group__clear-row a{
      color:#ffffff;
      Font-size: 18px;
      text-transform: uppercase;
      font-family:var(--base-font-family);
}

#shopify-section-{{section.id}} .filters__inner .filter-group.filter-group--applied{
     padding-left:20px;
}

#shopify-section-{{section.id}} .close-container .filters__close{ 
     color:#ffffff;
     Font-size: 18px;
     text-transform: uppercase;
     width:45%;
     display:block;
     text-align:center;
     font-style: normal;
     font-weight: 400;
     font-size: 18px;
     line-height: 20px;
     font-family:var(--base-font-family);
    
}
#shopify-section-{{section.id}} .link-dropdown{
     position:unset;
}
#shopify-section-{{section.id}} .product-block__title{
      font-size:14px;
      line-height:16px;
      padding-right: 30px;
}
 #shopify-section-{{section.id}} toggle-target {
       padding:10px 0;
       width: 100%;
       background: #F3F3F3;
}
#shopify-section-{{section.id}} .product-block__detail .price__current{
      font-size: 14px;
      line-height: 16px;
}
#shopify-section-{{section.id}} .product-block__detail .product-price--block .coupon-price{
      font-size: 14px;
      line-height: 16px;
}
#shopify-section-{{section.id}} .product-block__detail .price__was{
      font-size: 14px;
      line-height: 16px;
}
#shopify-section-{{section.id}} .product-label > span {
      font-size: 16px;
      line-height: 18px;
}
#shopify-section-{{section.id}} .price .price-label--sold-out{
     font-size: 14px;
    line-height: 16px;
}
}
@media screen and (max-width:420px){
  #shopify-section-{{section.id}} .product-block .collection-tag-label span{
  padding: 3px 3px 3px 9px;
  font-size:9px;
}
}
@media screen and (max-width:375px){
#shopify-section-{{section.id}} .product-block__detail .price__current{
      font-size: 12px;
      line-height: 14px;
}
#shopify-section-{{section.id}} .product-block__detail .product-price--block .coupon-price{
      font-size: 12px;
      line-height: 14px;
}
#shopify-section-{{section.id}} .product-block__detail .price__was{
      font-size: 12px;
      line-height: 14px;
}
#shopify-section-{{section.id}} .product-label > span {
      font-size: 12px;
      line-height: 14px;
}
#shopify-section-{{section.id}} .utility-bar{
     min-width:95%;
}
#shopify-section-{{section.id}} .link-dropdown__button[aria-expanded=true] + .link-dropdown__options {
  left:-2.5%;
}
}
#shopify-section-{{section.id}} .infinite-scroll-sentinel {
  min-height: 24px;
}
#shopify-section-{{section.id}} .infinite-scroll-spinner {
  display: block;
  width: 32px;
  height: 32px;
  margin: 0 auto 0.5rem;
  border: 2px solid rgba(0,0,0,0.1);
  border-top-color: #000;
  border-radius: 50%;
  animation: search-infinite-scroll-spin 0.8s linear infinite;
}
@keyframes search-infinite-scroll-spin {
  to { transform: rotate(360deg); }
}
</style>

{{ 'search.css' | asset_url | stylesheet_tag: preload: true }}
<script src="{{ 'search-page-size-inclusive.js' | asset_url }}" defer></script>
{% paginate search.results by section.settings.coll_num_per_page_int %} 
<div class="page-header" data-cc-animate>
  <div class="container">
    <h1 class="visually-hidden">{{ 'general.search.title' | t }}</h1>

    <div class="main-search">
      <div class="main-search__container">
        <form class="main-search__form" action="{{ routes.search_url }}" method="get" autocomplete="off">
          <div class="main-search__input-container">
            <input
              class="main-search__input"
              type="text"
              name="q"
              autocomplete="off"
              placeholder="{{ 'general.search.placeholder' | t | escape }}"
              aria-label="{{ 'general.search.input_label' | t | escape }}"
              value="{{ search.terms | escape }}" />
            <button class="main-search__button" type="submit" aria-label="{{ 'general.search.submit' | t }}">{% render 'icon-search' %}</button>
          </div>
          {%- if settings.show_search_suggestions -%}
            <script src="{{ 'search-suggestions.js' | asset_url }}" defer></script>
            <search-suggestions></search-suggestions>
          {%- endif -%}
        </form>
      </div>
    </div>
  </div>
</div>

{% if search.performed %}
  {% capture override_handle %}{% render 'collection-search-override-handle', terms: search.terms %}{% endcapture %}
  {%- liquid
    assign override_handle = override_handle | strip
    assign override_coll = blank
    assign search_override_ok = false
    if override_handle != blank and paginate.current_page == 1
      assign override_coll = collections[override_handle]
      if override_coll
        assign search_override_ok = true
      endif
    endif

    assign so_cap = section.settings.coll_num_per_page_int
    assign so_grid_count = 0
    if search_override_ok
      for product in override_coll.products
        if product.available
          unless product.tags contains 'no-search'
            assign so_grid_count = so_grid_count | plus: 1
            if so_grid_count >= so_cap
              break
            endif
          endunless
        endif
      endfor
    endif

    assign show_override_grid = false
    if search_override_ok and so_grid_count > 0
      assign show_override_grid = true
    endif

    assign show_filters = section.settings.enable_filtering
    if search.filters == empty
      assign show_filters = false
    endif

    assign no_search_count = 0
    for item in search.results
      if item.object_type == 'product'
        if item.tags contains 'no-search' or item.available == false
          assign no_search_count = no_search_count | plus: 1
        endif
      endif
    endfor
    assign base_visible = paginate.items | minus: no_search_count
    if base_visible < 0
      assign base_visible = 0
    endif
    assign visible_count = base_visible
    if search.results == empty and show_override_grid and so_grid_count > 0
      assign visible_count = so_grid_count
    endif
  -%}

  {% if visible_count == 0 and search.results != empty %}
    <div class="container">
      <h5 class="align-centre fully-spaced-row" data-cc-animate>{{ 'general.search.no_results' | t: terms: search.terms }}</h5>
    </div>
  {% else %}
  <div class="container{% if settings.use_fullwidth_layout %} container--no-max{% endif %}">
    {% liquid
      assign filter_count = 0
      for filter in search.filters
        if filter.param_name == 'filter.v.availability'
          continue
        endif
        assign filter_count = filter_count | plus: filter.active_values.size
        if filter.type == 'price_range'
          if filter.min_value.value != nil or filter.max_value.value != nil
            assign filter_count = filter_count | plus: 1
          endif
        endif
      endfor
    %}

    <div class="kisah-ctrl" data-ajax-container>
      <div class="kisah-ctrl__n"><b>{{ search.results_count }}</b> results</div>
      {%- if section.settings.show_sort -%}
        <button class="kisah-ctrl__btn" id="kisahSortBtn" type="button">
          <svg viewBox="0 0 24 24"><path d="M7 4v16M7 20l-3-3M7 20l3-3M17 20V4M17 4l-3 3M17 4l3 3"/></svg>
          Sort
        </button>
      {%- endif -%}
      {%- if section.settings.show_sort and show_filters -%}
        <span class="kisah-ctrl__rule"></span>
      {%- endif -%}
      {%- if show_filters -%}
        <button class="kisah-ctrl__btn kisah-ctrl__btn--filter" id="kisahFilterBtn" type="button" data-any="{% if filter_count > 0 %}1{% else %}0{% endif %}">
          <svg viewBox="0 0 24 24"><path d="M3 5h18M6 12h12M10 19h4"/></svg>
          Filter{% if filter_count > 0 %}<span class="kisah-ctrl__pip">{{ filter_count }}</span>{% endif %}
        </button>
      {%- endif -%}
    </div>

    {%- if filter_count > 0 -%}
      <div class="kisah-applied" id="kisahApplied" data-ajax-container>
        {%- for filter in search.filters -%}
          {%- if filter.param_name == 'filter.v.availability' -%}
            {%- continue -%}
          {%- endif -%}
          {%- for value in filter.active_values -%}
            <a class="kisah-achip" href="{{ value.url_to_remove }}">
              {{ value.label }}
              <span class="close-x">&times;</span>
            </a>
          {%- endfor -%}
          {%- if filter.type == 'price_range' and filter.min_value.value != nil or filter.type == 'price_range' and filter.max_value.value != nil -%}
            <a class="kisah-achip" href="{{ filter.url_to_remove }}">
              {{ filter.min_value.value | default: 0 | money }} - {{ filter.max_value.value | default: filter.range_max | money }}
              <span class="close-x">&times;</span>
            </a>
          {%- endif -%}
        {%- endfor -%}
        {% assign clear_url = search.terms | escape | prepend: '?q=' | prepend: routes.search_url %}
        <a class="kisah-achip kisah-achip--clear" href="{{ clear_url }}">Clear all</a>
      </div>
    {%- endif -%}

    <filter-container class="filter-container filter-container--side{% if section.settings.collapse_tag_sidebar_by_default == false %} filter-container--show-filters-desktop{% endif %}" data-ajax-filtering="{{ settings.ajax_products }}" data-filter-section-id="{{ section.id }}">
      {% liquid
        assign clear_url = search.terms | escape | prepend: '?q=' | prepend: routes.search_url
        if section.settings.show_sort or show_filters
          render 'faceted-filters', filter_context: search, clear_url: clear_url, current_sort: current_sort
        endif
      %}

      <div class="filters-adjacent collection-listing" data-ajax-container>
        {% if search.results == empty and so_grid_count == 0 %}
          <h5 class="align-centre fully-spaced-row" data-cc-animate>{{ 'general.search.no_results' | t: terms: search.terms }}</h5>
        {% else %}
          <div class="product-grid product-grid--per-row-{{ section.settings.grid }} product-grid--per-row-mob-{{ settings.prod_thumb_mob_per_row }} search-infinite-scroll-grid" data-size-limit="{{ section.settings.coll_num_per_page_int }}">
            {%- liquid
              assign so_aspect_products = false
              if search.results == empty and show_override_grid
                assign so_aspect_products = true
              endif

              if settings.prod_thumb_shape == 'portrait-23'
                assign chosen_product_aspect_ratio = 0.67
              elsif settings.prod_thumb_shape == 'portrait-45'
                assign chosen_product_aspect_ratio = 0.8
              elsif settings.prod_thumb_shape == 'square'
                assign chosen_product_aspect_ratio = 1.0
              elsif settings.prod_thumb_shape == 'landscape-54'
                assign chosen_product_aspect_ratio = 1.25
              elsif settings.prod_thumb_shape == 'landscape-32'
                assign chosen_product_aspect_ratio = 1.50
              elsif settings.prod_thumb_shape == 'tallest'
                assign chosen_product_aspect_ratio = 99
                if so_aspect_products
                  for product in override_coll.products
                    if product.available
                      unless product.tags contains 'no-search'
                        if product.featured_media.preview_image.aspect_ratio < chosen_product_aspect_ratio
                          assign chosen_product_aspect_ratio = product.featured_media.preview_image.aspect_ratio
                        endif
                      endunless
                    endif
                  endfor
                else
                  for product in search.results
                    if product.object_type == 'product' and product.available
                      unless product.tags contains 'no-search'
                        if product.featured_media.preview_image.aspect_ratio < chosen_product_aspect_ratio
                          assign chosen_product_aspect_ratio = product.featured_media.preview_image.aspect_ratio
                        endif
                      endunless
                    endif
                  endfor
                endif
              else
                assign chosen_product_aspect_ratio = 0
                if so_aspect_products
                  for product in override_coll.products
                    if product.available
                      unless product.tags contains 'no-search'
                        if product.featured_media.preview_image.aspect_ratio > chosen_product_aspect_ratio
                          assign chosen_product_aspect_ratio = product.featured_media.preview_image.aspect_ratio
                        endif
                      endunless
                    endif
                  endfor
                else
                  for product in search.results
                    if product.object_type == 'product' and product.available
                      unless product.tags contains 'no-search'
                        if product.featured_media.preview_image.aspect_ratio > chosen_product_aspect_ratio
                          assign chosen_product_aspect_ratio = product.featured_media.preview_image.aspect_ratio
                        endif
                      endunless
                    endif
                  endfor
                endif
              endif

              if settings.coll_grid_image_shape == 'portrait-23'
                assign chosen_collection_aspect_ratio = 0.67
              elsif settings.coll_grid_image_shape == 'portrait-45'
                assign chosen_collection_aspect_ratio = 0.8
              elsif settings.coll_grid_image_shape == 'square'
                assign chosen_collection_aspect_ratio = 1.0
              elsif settings.coll_grid_image_shape == 'landscape-54'
                assign chosen_collection_aspect_ratio = 1.25
              elsif settings.coll_grid_image_shape == 'landscape-32'
                assign chosen_collection_aspect_ratio = 1.50
              elsif settings.coll_grid_image_shape == 'tallest'
                assign chosen_collection_aspect_ratio = 99
                for collection in search.results
                  if collection.object_type == 'collection'
                    if collection.featured_image.aspect_ratio < chosen_collection_aspect_ratio
                      assign chosen_collection_aspect_ratio = collection.featured_image.aspect_ratio
                    endif
                  endif
                endfor
              else
                assign chosen_collection_aspect_ratio = 0
                for collection in search.results
                  if collection.object_type == 'collection'
                    if collection.featured_image.aspect_ratio > chosen_collection_aspect_ratio
                      assign chosen_collection_aspect_ratio = collection.featured_image.aspect_ratio
                    endif
                  endif
                endfor
              endif
            -%}
            {%- if show_override_grid and so_grid_count > 0 -%}
              {%- assign so_id_prefix = ',' -%}
              {%- assign so_rendered = 0 -%}
              {%- for product in override_coll.products -%}
                {%- if product.available -%}
                  {%- if product.tags contains 'no-search' -%}{%- continue -%}{%- endif -%}
                  {% render 'product-block', product: product, collection: override_coll, custom_aspect_ratio: chosen_product_aspect_ratio, animate: true %}
                  {%- assign so_id_prefix = so_id_prefix | append: product.id | append: ',' -%}
                  {%- assign so_rendered = so_rendered | plus: 1 -%}
                  {%- if so_rendered >= so_cap -%}{%- break -%}{%- endif -%}
                {%- endif -%}
              {%- endfor -%}
            {%- endif -%}
            {% for item in search.results %}

  {% if item.object_type == 'product' %}
    {% if item.tags contains 'no-search' or item.available == false %}
      {% continue %}
    {% endif %}
    {%- if show_override_grid and so_grid_count > 0 -%}
      {%- assign so_needle = ',' | append: item.id | append: ',' -%}
      {%- if so_id_prefix contains so_needle -%}{%- continue -%}{%- endif -%}
    {%- endif -%}
    {% render 'product-block', product: item, custom_aspect_ratio: chosen_product_aspect_ratio, animate: true %}
  {% elsif item.object_type == 'collection' %}
    {% render 'collection-block', collection: item, custom_aspect_ratio: chosen_collection_aspect_ratio, animate: true %}
  {% else %}
    <div class="product-block page-block" data-cc-animate>
      <div class="block-inner">
        <div class="block-inner-inner">
          {% if item.object_type == 'article' and item.image %}
            <div class="article-image">
              <a href="{{ item.url }}">
                {%- render 'image' with item.image, size_cols_mobile: 2, size_cols_desktop: section.settings.grid, custom_aspect_ratio: 1.33 -%}
              </a>
            </div>
          {% endif %}
          <div class="text-cont">
            <h6 class="page-block__title"><a href="{{ item.url }}">{{ item.title }}</a></h6>
            {% unless item.object_type == 'article' and item.image %}
              <p>{{ item.content | strip_html | truncatewords: 20 }}</p>
            {% endunless %}
            <a href="{{ item.url }}" class="more">
              <span class="beside-svg underline">{{ 'blogs.article.read_more' | t }}</span>
              <span class="icon--small icon-natcol has-ltr-icon">{% render 'icon-chevron-right' %}</span>
            </a>
          </div>
        </div>
      </div>
    </div>
  {% endif %}
{% endfor %}
          </div>
        {% endif %}
      </div>
    </filter-container>
  </div>

  <div class="container pagination-row search-infinite-scroll-wrap" data-ajax-container>
    {% if paginate.next %}
      <div class="infinite-scroll-sentinel" data-next-url="{{ paginate.next.url }}" data-section-id="{{ section.id }}" aria-hidden="true">
        <div class="infinite-scroll-loading" style="display: none; text-align: center; padding: 0.5rem;">
          <span class="infinite-scroll-spinner"></span>
        </div>
      </div>
    {% endif %}
  </div>
  {% endif %}
{% endif %}

{% endpaginate %}

<script>
(function() {
  'use strict';
  function initSearchInfiniteScroll() {
    var grid = document.querySelector('.search-infinite-scroll-grid');
    var sentinel = document.querySelector('.search-infinite-scroll-wrap .infinite-scroll-sentinel[data-next-url]');
    if (!grid || !sentinel) return;
    if (sentinel._infiniteScrollObserved) return;
    sentinel._infiniteScrollObserved = true;

    var loading = false;

    function loadMore() {
      var nextUrl = sentinel.getAttribute('data-next-url');
      if (!nextUrl || loading) return;

      loading = true;
      var loadingEl = sentinel.querySelector('.infinite-scroll-loading');
      if (loadingEl) loadingEl.style.display = 'block';

      var sectionId = sentinel.getAttribute('data-section-id');
      var fetchUrl = nextUrl + (nextUrl.indexOf('?') >= 0 ? '&' : '?') + 'section_id=' + encodeURIComponent(sectionId);

      fetch(fetchUrl)
        .then(function(res) { return res.text(); })
        .then(function(html) {
          var parser = new DOMParser();
          var doc = parser.parseFromString(html, 'text/html');
          var newGrid = doc.querySelector('.search-infinite-scroll-grid');
          var newSentinel = doc.querySelector('.infinite-scroll-sentinel');

          if (newGrid && newGrid.children.length) {
            for (var i = 0; i < newGrid.children.length; i++) {
              grid.appendChild(newGrid.children[i].cloneNode(true));
            }
          }

          if (newSentinel && newSentinel.getAttribute('data-next-url')) {
            sentinel.setAttribute('data-next-url', newSentinel.getAttribute('data-next-url'));
            if (newSentinel.getAttribute('data-section-id')) sentinel.setAttribute('data-section-id', newSentinel.getAttribute('data-section-id'));
          } else {
            sentinel.removeAttribute('data-next-url');
            sentinel.parentNode.style.display = 'none';
          }
        })
        .catch(function() {
          sentinel.removeAttribute('data-next-url');
          if (loadingEl) loadingEl.style.display = 'none';
        })
        .finally(function() {
          loading = false;
          if (loadingEl) loadingEl.style.display = 'none';
        });
    }

    var observer = new IntersectionObserver(function(entries) {
      for (var i = 0; i < entries.length; i++) {
        if (entries[i].isIntersecting && sentinel.getAttribute('data-next-url')) {
          loadMore();
          break;
        }
      }
    }, { rootMargin: '0px 0px 300px 0px', threshold: 0 });

    observer.observe(sentinel);
  }

  initSearchInfiniteScroll();
  var wrap = document.querySelector('.search-infinite-scroll-wrap');
  if (wrap && typeof MutationObserver !== 'undefined') {
    var mo = new MutationObserver(function() {
      initSearchInfiniteScroll();
    });
    mo.observe(wrap, { childList: true, subtree: true });
  }
})();
</script>

{% schema %}
{
  "name": "Search results",
  "class": "section-search-template page-section-spacing",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_vendor",
      "label": "Show product vendors",
      "default": false
    },
    {
      "type": "range",
      "id": "coll_num_per_page_int",
      "min": 16,
      "max": 50,
      "step": 2,
      "label": "Number of products to show per page",
      "default": 24
    },
    {
      "type": "range",
      "id": "grid",
      "label": "Products per row",
      "min": 2,
      "max": 5,
      "step": 1,
      "default": 4
    },
    {
      "type": "header",
      "content": "Filters"
    },
    {
      "type": "checkbox",
      "id": "enable_filtering",
      "label": "Enable filtering",
      "info": "[Customize filters](/admin/menus)",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "collapse_tag_sidebar_by_default",
      "label": "Collapse filter column",
      "default": false
    },
    {
      "type": "select",
      "id": "collapse_filters_method",
      "label": "Filter row collapse",
      "default": "over-12",
      "options": [
        {
          "value": "none",
          "label": "None"
        },
        {
          "value": "over-6",
          "label": "Over 6 items"
        },
        {
          "value": "over-12",
          "label": "Over 12 items"
        },
        {
          "value": "over-18",
          "label": "Over 18 items"
        },
        {
          "value": "all",
          "label": "All rows"
        }
      ]
    },
    {
      "type": "checkbox",
      "id": "show_filter_counts",
      "label": "Show filter counts",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_disabled_filters",
      "label": "Show filters with zero results",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "auto_apply_hide_unavailable",
      "label": "On selecting a filter, automatically apply 'Hide out of stock'",
      "default": false,
      "info": "Requires Availability filter to be enabled"
    },
    {
      "type": "checkbox",
      "id": "enable_sticky_filter",
      "label": "Enable stick on scroll",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "filter_two_columns",
      "label": "Two column filter values",
      "default": false
    },
    {
      "type": "header",
      "content": "Sorting"
    },
    {
      "type": "checkbox",
      "id": "show_sort",
      "label": "Show sorting dropdown",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sort_show_featured",
      "label": "Enable order: Featured",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "sort_show_best",
      "label": "Enable order: Best selling",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sort_show_alpha",
      "label": "Enable order: Alphabetical",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sort_show_price",
      "label": "Enable order: Price",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sort_show_date",
      "label": "Enable order: Date added",
      "default": true
    }
  ]
}
{% endschema %}
<style>
  body.template-search .product-block.main-search-result.out-of-stock {
    display: block !important;
}
</style>