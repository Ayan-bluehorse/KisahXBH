<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>
  {%- if request.page_type == 'password' -%}
    {{- shop.name -}}
  {%- else -%}
    {{- page_title -}}
    {%- if current_tags %}{% assign meta_tags = current_tags | join: ', ' %} &ndash; {{ 'general.meta.tags' | t: tags: meta_tags }}{% endif -%}
    {%- if current_page != 1 %} &ndash; {{ 'general.meta.page' | t: page: current_page }}{% endif -%}
    {%- unless page_title contains shop.name %} &ndash; {{ shop.name }}{% endunless -%}
  {%- endif -%}
</title>

{%- liquid
  assign clean_canonical = canonical_url
  if clean_canonical contains '/en-us/'
    assign clean_canonical = clean_canonical | replace: '/en-us/', '/'
  elsif clean_canonical contains '/en-us'
    assign clean_canonical = clean_canonical | replace: '/en-us', ''
  endif
-%}
{%- if clean_canonical != blank -%}
  <link rel="canonical" href="{{ clean_canonical }}">
  <link rel="alternate" hreflang="en-IN" href="{{ clean_canonical }}">
  <link rel="alternate" hreflang="x-default" href="{{ clean_canonical }}">
{%- endif -%}

{%- if settings.favicon != blank -%}
  {%- liquid
    assign image_type = settings.favicon | split: '?' | first | split: '.' | last | downcase
    if image_type == 'svg'
      assign image_type = 'svg+xml'
    endif
  -%}
  <link rel="icon" href="{{ settings.favicon | image_url: width: 48, height: 48 }}" type="image/{{ image_type }}">
  <link rel="apple-touch-icon" href="{{ settings.favicon | image_url: width: 180, height: 180 }}">
{%- endif -%}

{%- if page_description -%}
  <meta name="description" content="{{ page_description | escape }}">
{%- endif -%}
